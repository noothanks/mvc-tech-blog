<div>
    <h2 class="card-header">Comments:</h2>
  {{#each this}}
  <section class="list-item">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>